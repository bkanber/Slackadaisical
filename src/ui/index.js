const ChannelBox = require('./ChannelBox');
const ChannelsList = require('./ChannelsList');
const MessageForm = require('./MessageForm');
const MessagesList = require('./MessagesList');

module.exports = {
    ChannelBox,
    ChannelsList,
    MessageForm,
    MessagesList,
};
